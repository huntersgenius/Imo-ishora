"""Request and response models (Pydantic v2).

Owned by Part 03 (Backend Core). The schema layer maps the framework-free
domain models in ``app.services.models`` to the public API surface so the
linguistic core never depends on the web framework. Stub at end of Part 02.
"""
