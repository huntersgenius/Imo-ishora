"""Service layer.

The dictionary and text-processing services are deliberately framework-free
(standard library only). They form the linguistic core and must remain
importable and testable without the web stack installed.
"""
